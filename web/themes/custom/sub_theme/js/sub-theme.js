/**
 * @file
 * Sub Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.subTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
